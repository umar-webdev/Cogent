# Copy to .env.local for local development (never commit secrets).

# Client-visible env vars must be prefixed with VITE_
VITE_APP_ENV=development

# Future — thin API (Phase 6)
# VITE_API_URL=http://localhost:3001
